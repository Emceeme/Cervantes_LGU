# For Render: Use Docker build
# Render will automatically use the Dockerfile if present
# This Procfile is for Heroku compatibility

# For Heroku deployment:
web: heroku-php-apache2 -C .htaccess

# For Render deployment with native PHP (not Docker):
# web: php -S 0.0.0.0:$PORT -t .
